# Base runtime dependencies (aligned with pyproject.toml)
numpy>=1.23.0
pandas>=1.5.0
PyYAML>=6.0.0
requests>=2.31.0
psutil>=5.9.0
networkx>=3.0
matplotlib>=3.7.0
plotly>=5.0.0

# Optional runtime extras
nvidia-ml-py>=12.0.0    # [gpu]
torch>=2.1.0            # [ml]
streamlit>=1.30.0       # [ui]

# Dev/test tooling
pytest>=8.0
pytest-cov>=5.0.0
pytest-mock>=3.12.0
black>=24.0.0
flake8>=7.0.0
isort>=5.13.0
mypy>=1.8.0
ruff>=0.6.0
bandit>=1.7.0
safety>=3.0.0
pre-commit>=3.8.0

# Optional: PDF report generation (install one for best results)
# playwright  # Best quality - pip install playwright && playwright install chromium
# pdfkit      # Alternative - also requires wkhtmltopdf system package
# weasyprint  # Fallback - no JavaScript/chart support
